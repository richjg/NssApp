<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NssApp.Views.SplashScreenView"
             BackgroundColor="{StaticResource Color_VeritasRed}">
    <ContentPage.Content>
        <Grid>
            <Label HorizontalOptions="CenterAndExpand" Text="Welcome to" FontSize="Micro" TextColor="White" />
            <Label HorizontalOptions="CenterAndExpand" Text="NetBackup Self Service" FontSize="Large" TextColor="White" />
            <ActivityIndicator HorizontalOptions="CenterAndExpand" Color="White" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        </Grid>
        <!--<StackLayout HorizontalOptions="CenterAndExpand" Margin="0, 50, 0, 0">
            <Label HorizontalOptions="CenterAndExpand" Text="Welcome to" FontSize="Micro" TextColor="White" />
            <Label HorizontalOptions="CenterAndExpand" Text="NetBackup Self Service" FontSize="Large" TextColor="White" />
            <ActivityIndicator HorizontalOptions="CenterAndExpand" Color="White" IsRunning="True" />
        </StackLayout>-->
    </ContentPage.Content>
</ContentPage>